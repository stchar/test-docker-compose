# YAExample of ngnix load balancer
Docker compose example of nginx lb and some static html

## run
```
docker-compose up -d
```

## verify
```
curl -L http://localhost:8080 && curl -L http://localhost:8080
```